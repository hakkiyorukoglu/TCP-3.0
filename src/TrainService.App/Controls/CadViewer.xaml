<UserControl x:Class="TrainService.App.Controls.CadViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             ClipToBounds="True">
    <Grid Background="#1E1E1E" MouseWheel="Grid_MouseWheel" MouseRightButtonDown="Grid_MouseRightButtonDown" MouseRightButtonUp="Grid_MouseRightButtonUp" MouseMove="Grid_MouseMove">
        <Canvas x:Name="MapCanvas" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
            <Canvas.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="MapScale" ScaleX="1" ScaleY="1"/>
                    <TranslateTransform x:Name="MapTranslate" X="0" Y="0"/>
                </TransformGroup>
            </Canvas.RenderTransform>
        </Canvas>
        
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10">
            <TextBlock Text="Pan: Sağ Tık (Sürükle)" Foreground="Gray" FontSize="12"/>
            <TextBlock Text="Zoom: Tekerlek" Foreground="Gray" FontSize="12"/>
        </StackPanel>
    </Grid>
</UserControl>

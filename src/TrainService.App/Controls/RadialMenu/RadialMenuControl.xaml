<UserControl x:Class="TrainService.App.Controls.RadialMenu.RadialMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="Root">
    <Popup x:Name="MenuPopup"
           Placement="Absolute"
           AllowsTransparency="True"
           PopupAnimation="Fade"
           StaysOpen="False"
           Closed="OnPopupClosed">
        <Border Background="Transparent">
            <Canvas x:Name="MenuCanvas"
                    Width="280" Height="280"
                    MouseMove="OnCanvasMouseMove"
                    MouseLeftButtonUp="OnCanvasMouseLeftButtonUp"
                    MouseLeave="OnCanvasMouseLeave">
                <!-- Slices are drawn programmatically via DrawingContext/Shapes -->
            </Canvas>
        </Border>
    </Popup>
</UserControl>
